import React from 'react';

const GroupingButtons = () => {
  return (
    <div className="grouping-buttons">
      <button>Group by Status</button>
      <button>Group by User</button>
      <button>Group by Priority</button>
    </div>
  );
};

export default GroupingButtons;
